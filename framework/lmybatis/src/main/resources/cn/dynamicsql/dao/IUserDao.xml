<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dynamicsql.dao.IUserDao">
    <resultMap id="defaultUserWithAccountMap" type="cn.dynamicsql.domain.User">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="birthday" column="birthday"/>
        <result property="sex" column="sex"/>
        <result property="address" column="address"/>
        <collection property="accountList" ofType="cn.dynamicsql.domain.Account">
            <result property="id" column="id"/>
            <result property="uid" column="uid"/>
            <result property="money" column="money"/>
        </collection>
    </resultMap>

    <resultMap id="userWithAccountMap" type="cn.dynamicsql.domain.User">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="birthday" column="birthday"/>
        <result property="sex" column="sex"/>
        <result property="address" column="address"/>
        <collection property="accountList" ofType="cn.dynamicsql.domain.Account" select="cn.dynamicsql.dao.IAccountDao.findByUid" column="id">
        </collection>
    </resultMap>


    <sql id="defaultselect">
        select * from user
    </sql>

    <select id="findByCondition" parameterType="cn.dynamicsql.domain.User" resultType="cn.dynamicsql.domain.User">
        select * from user where 1=1
        <if test="id != null">
          and id = #{id}
        </if>
        <if test="username != null and username != ''">
            and username = #{username}
        </if>
        <if test="birthday != null and birthday != ''">
            and birthday = #{birthday}
        </if>
        <if test="sex != null and sex != ''">
            and sex = #{sex}
        </if>
        <if test="address != null and address != ''">
            and address = #{address}
        </if>
    </select>

    <select id="findById" resultType="cn.dynamicsql.domain.User" parameterType="INT">
      <include refid="defaultselect" />
        <where>

          <if test="_parameter != null">
              and id = #{id}
          </if>

        </where>
    </select>
    
    <select id="findBylis" resultType="cn.dynamicsql.domain.User" parameterType="java.util.List">
        <include refid="defaultselect"/>
        <where>
            <if test="array != null">
                <foreach collection="array" open="and id in (" close=")" item="id" separator=",">
                    #{id}
                </foreach>
            </if>
        </where>
    </select>

    <select id="findAllwithAccount" resultMap="userWithAccountMap">
        select u.*
        from user u
    </select>

</mapper>