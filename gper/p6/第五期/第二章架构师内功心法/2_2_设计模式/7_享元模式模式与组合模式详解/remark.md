# 享元模式的定义
```java
享元模式（Flyweigth Pattern）又称为轻量级模式，是对象池的一种实现，
类似于线程池，线程池可以避免不停的创建和销毁多个对象，消耗性能，提供了减少
对象数量从而改善应用所需的对象结构的方式

宗旨：共享细粒度对象，将多个对同一对象的访问集中起来

属于结构型模式
```

# 享元模式的适用场景
```java
常常应用于系统底层的开发，以便解决系统的性能问题。系统大量相似对象，需要缓冲池的场景
``` 

# 享元模式的优点
```java
减少对象的创建，降低内存中对象的数量，降低系统的内存，提高效率
减少内存之外的其他资源占用
```

# 享元模式缺点
```java
关注内，外部状态，关注线程安全问题
使用系统，程序的逻辑复杂化
```

# 组合模式的定义
```java
组合模式（Composite Pattern）也称为整体-部分（Part-Whole）模式，它的宗旨是
通过单个对象（叶子节点）和组合对象（树枝节点）用相同的接口进行表示

作用：使用客户端对单个对象和组合对象保持一致的方式处理。

属于结构型模式。
```
!p[](image/Snipaste_2021-03-27_21-13-34.png)


# 组合模式的适用场景
```java
1, 希望客户端可以 忽略组合对象与单个对象的差异时；
2，对象层次具备整体和部分，呈树形结构（如树形菜单，操作系统目录结构，公司组织架构等）。
```
# 组合模式的优点
```java
1，清楚地定义分层次的复杂对象， 表示对象的全部或部分层次
2，让客户端忽略了层次的差异，方便对整个层次结构进行控制
3，简化客户端代码
4，符合开闭原则
```

# 组合模式的缺点
```java
1，限制类型时会较为复杂
2，使设计变得更加抽象
```
